version: '3.7'
services:
  joinfut-backend:
    image: joinfut/backend:latest
    ports:
      - "8080:8080"
    networks:
      - backend
    environment:
      - DB_URL=jdbc:postgresql://_______________:5433/joinfut
      - DB_DDL=UPDATE

networks:
  backend:
    external: true
    driver: bridge